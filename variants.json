{
  "$comment": "Profile definitions for setup.mjs. Globs relative to payload/. exclude wins over include; extends unions the parent chain's exclude first. optional groups are promoted over exclude when active.",
  "alwaysExclude": ["hooks/task-lifecycle-probe*", "claude-md/**", "**.test.mjs"],
  "managedPlugins": {
    "ultrapowers":  "ultrapowers@ultrapowers",
    "superpowers":  "superpowers@claude-plugins-official",
    "gsd":          "gsd@claude-plugins-official",
    "context-mode": "context-mode@context-mode",
    "context7":     "context7@claude-plugins-official"
  },
  "$marketplaces": "Where each managed plugin's marketplace comes from, so setup.mjs can register it before installing - `claude plugin install` fails outright against an unknown marketplace. Sources are transcribed from a real ~/.claude/plugins/known_marketplaces.json, not guessed; a marketplace with no entry here produces a note telling the human to add one, never an invented repo. This gap predates the fork: the four pre-fork plugins live in marketplaces that any machine which ran the bootstrap already has, so nothing ever tripped it.",
  "marketplaces": {
    "ultrapowers":             "axazolai/ultrapowers",
    "claude-plugins-official": "anthropics/claude-plugins-official",
    "context-mode":            "mksglu/context-mode"
  },
  "$gsd": "`gsd` stays in managedPlugins but appears in no profile, so plugin-reconcile prints its uninstall command rather than its install command. GSD-Core is installed with npx, not from a marketplace, and the marketplace plugin of that name was never what this bundle wanted - it was carried in the full profile by inheritance. Removing the entry entirely would be quieter but weaker: a plugin nobody manages is a plugin nobody offers to remove, and a machine that already has it would keep it forever without ever being told.",
  "$context7": "Forbidden, not merely unrequired. Context7 is reached through its MCP server; the marketplace plugin of the same name shadows that, so it is removed on sight rather than left disabled. It stays in managedPlugins because a plugin nobody manages is a plugin nobody can find and remove.",
  "$forbiddenPlugins": "Must not exist on the machine at all. Stronger than absence from a profile: plugin-reconcile refuses to install or enable these no matter what a profile asks for, ignores keepInstalled for them, and uninstalls (removes files) rather than disabling. Every name here must also be in managedPlugins, and no profile may list one - both are asserted in variants.test.mjs.",
  "forbiddenPlugins": ["context7"],
  "$keepInstalled": "Managed plugins that are disabled when not required, but never uninstalled. superpowers is here because ultrapowers is our fork of it: keeping upstream on disk makes rollback one command (re-enable it, disable the fork) instead of a reinstall from the marketplace. It must stay in managedPlugins to be disabled at all - a plugin we stop managing is a plugin we stop turning off. Both enabled at once is NOT the fallback: they share 14 skill names and that is undocumented behaviour we do not run in production. ultrapowers is here for `lite`, which ships it to disk but does not enable it.",
  "$gsdCore": "The gsd-core release this bundle was validated against. Pinned, not @latest: the gsd-executor fork is regenerated from one exact release, the twelve agent patches are verified against it, and a hook patch anchors a line in it - @latest would hand two machines set up on different days a different gsd-core and silently invalidate that on one of them. setup.mjs installs or upgrades TO this version and never downgrades; an install ahead of it is reported, not touched. Bumping this is the moment to re-verify the fork and the patches (see RISK-GSDEXEC-001).",
  "gsdCore": { "version": "1.11.0" },
  "keepInstalled": ["superpowers", "ultrapowers"],
  "profiles": {
    "full": { "plugins": ["ultrapowers", "context-mode"] },

    "base": {
      "plugins": ["ultrapowers", "context-mode"],
      "exclude": [
        "agents/gsd-*.md", "apply-gsd-agent-patches.mjs", "gsd-defaults-sync.mjs",
        "sync-gsd-context-mode-tool.mjs", "commands/init-session.md",
        "hooks/gsd-*", "hooks/lib/gsd-*", "hooks/lib/context-mode-gsd-agents.mjs",
        "hooks/worktree-executor-discipline-advisor.mjs",
        "rules-src/gsd.md", "skills/using-git-worktrees/**",
        "references/**",
        "hooks/db-live-access-gate.mjs", "hooks/ci-watch-nudge*"
      ]
    },

    "lite": {
      "extends": "base",
      "plugins": ["context-mode"],
      "overlay": "payload-lite",
      "maxPluginTier": "core",
      "exclude": [
        "hooks/bg-supervision-nudge*", "bin/supervise-bg.mjs", "bin/lib/supervise-lib*",
        "commands/init-mcp.md",
        "hooks/schedulewakeup-loop-only-nudge*",
        "hooks/pnpm-phantom-fix-hook*", "bin/pnpm-phantom-*", "bin/lib/pnpm-*",
        "bin/turbopack-gvs-check.mjs", "bin/lib/turbopack-gvs-*", "commands/pnpm-phantom-fix.md"
      ]
    }
  }
}
